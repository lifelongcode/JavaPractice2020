Q. How to use lambda expression.
Get all elements' grater than 2,
sort them and then push them to a new set,
using lambda expression.

Maybe given
Set<Integer> intSet = new HashSet<>();

//Add elements onto it
intSet.add(1);
intSet.add(2);
intSet.add(3);
intSet.add(4);
intSet.add(5);


//Note I think we can add set element using for loop
for(int i=0; i<5; i++){
intSet.add(i);
}
